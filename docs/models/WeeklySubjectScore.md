# WeeklySubjectScore

**Source:** `com.examprep.model.WeeklySubjectScore`  
**Table:** `weekly_subject_scores`

Per-subject snapshot after the official weekly submit. Same bands as diagnostic (`STRONG` ≥ 75%, `DEVELOPING` 50–74%, `WEAK` < 50%). Used to weight the next week’s form.
