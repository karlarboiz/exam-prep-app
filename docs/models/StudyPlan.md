# StudyPlan

**Source:** `com.examprep.model.StudyPlan`

View model for `/user/study-plan` and the dashboard card: regimen, subject bands, missed answers, 3–5 focus targets, email digest flag. If the current week has no official score, bands fall back to the last completed week or the diagnostic snapshot (`fromDiagnostic`).
