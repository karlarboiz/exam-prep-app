# UI

Visual layout and mobile readability.

| Feature | Status | Notes |
|---------|--------|-------|
| Mobile stacked tables | Done | `.data-table` becomes labeled stacked rows at ≤768px; `data-label` on body cells |
